#if os(macOS)
import Foundation

// Provides a stable sort by source location for build errors
// Note: This extension is redundant with the one in CompilerError+Convenience.swift
// but kept for backwards compatibility with any code that imports this separately
#endif
